name: wechat_camera_picker_demo
description: A new Flutter project.
version: 4.5.0+45
publish_to: none

environment:
  sdk: ^3.3.0
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: # transitive

  wechat_camera_picker:
    path: ../

  package_info_plus: '>=7.0.0 <10.0.0'
  video_player: ^2.7.0

dev_dependencies:
  flutter_lints: any

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
